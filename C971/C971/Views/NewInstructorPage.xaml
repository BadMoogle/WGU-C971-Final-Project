<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:sys="clr-namespace:System;assembly=netstandard"
             mc:Ignorable="d"
             x:Class="C971.Views.NewInstructorPage"
             Title="New Instructor">
    
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Cancel" Clicked="Cancel_Clicked" />
        <ToolbarItem Text="Save" Clicked="Save_Clicked" />
    </ContentPage.ToolbarItems>
    
    <ContentPage.Content>
        <StackLayout Spacing="20" Padding="15" BindingContext="{Binding Teacher}">
            <Label Text="Instructor Name" FontSize="Medium" />
            <Entry Text="{Binding Name}" d:Text="Instructor Name" FontSize="Small" />
            <Label Text="Email Address" FontSize="Medium" />
            <Entry Text="{Binding Email}" d:Text="Email Address" FontSize="Small" />
            <Label Text="PHone Number" FontSize="Medium" />
            <Entry Text="{Binding PhoneNumber}" d:Text="Phone Number" FontSize="Small" />
        </StackLayout>
    </ContentPage.Content>
    
</ContentPage>
